# transport/zmq.yaml

# 내부 프로세스 간 통신(IPC)을 위한 ZeroMQ 설정
# 각 데이터 타입별로 독립된 소켓 주소(Endpoint)를 가집니다.

type: zmq
bundle_endpoint: tcp://127.0.0.1:5555  # Frame + Odom bundle 전송용 주소
imu_endpoint: tcp://127.0.0.1:5556     # (향후 확장용) IMU 센서 전송용 주소
map_endpoint: tcp://127.0.0.1:5558     # Map Slice 전송용 주소